# Traefik without Docker socket (single-container / RunPod).
api:
  dashboard: false
  insecure: false

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/dynamic.yaml
    watch: true

log:
  level: INFO
